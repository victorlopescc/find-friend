import { FastifyInstance } from 'fastify'

// import orgs controllers

export async function orgsRoutes(app: FastifyInstance) {
    // POST /orgs
    // POST /orgs/authenticate
    // GET /orgs/nearby
}
